<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Readify</title>
    <link rel="stylesheet" href="/css/boilerplate.css">
</head>
<body>
    <div class="pcontainer">
        <div class="navbar">
            <div class="ReadifyLogo">
                <h3>Readify</h3>
            </div>
            <div class="Chats">
                <% for( Chat of userChats) { %>
                    <a href="/<%= Chat.id %>"><%= Chat.id %></a>
                    <br>
                <% } %>
            </div>
        </div>
        <div class="container">
            <%- body %>
        </div>
    </div>
    
</body>
</html>